import { IRouterContext } from 'koa-router'
import md5password from '/@/utils/password-handle'

export const verifyLogin = (ctx: IRouterContext, next) => {}

export const login = (ctx: IRouterContext, next) => {}
